# SPAR GPU Telemetry Pipeline

# GPU monitoring (Tier 1 NVML)
nvidia-ml-py3>=7.352.0

# Data handling
pandas>=2.0.0
pyarrow>=14.0.0

# ML workloads (needed on instances running training/inference workloads)
torch>=2.1.0
torchvision>=0.16.0

# Utilities
tqdm>=4.65.0

# Note: DCGM Python bindings (Tier 2) come from the datacenter-gpu-manager
# system package, not pip. Install via: apt-get install datacenter-gpu-manager
